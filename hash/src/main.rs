fn main() {
    use std::collections::HashMap;

    let mut scores: HashMap<String, u32> = HashMap::new();
    scores.insert(String::from("電通大"), 10);
    scores.insert(String::from("でんとぅー大"), 100);

    println!("Hello, world!");
}
